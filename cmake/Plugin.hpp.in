#pragma once

#include <string_view>

namespace Plugin
{
	using namespace std::literals;

	inline constexpr auto NAME = "@PROJECT_NAME@"sv;
	inline constexpr auto AUTHOR = "@PROJECT_AUTHOR@"sv;
	inline constexpr auto VERSION = REL::Version{ @PROJECT_VERSION_MAJOR@u, @PROJECT_VERSION_MINOR@u, @PROJECT_VERSION_PATCH@u, @PROJECT_VERSION_TWEAK@u };
}
